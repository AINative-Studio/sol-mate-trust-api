{
  "name": "sol-mate-escrow-tests",
  "version": "0.1.0",
  "scripts": {
    "test": "ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts",
    "build": "anchor build",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "idl:upgrade": "anchor idl upgrade --provider.cluster devnet --filepath target/idl/sol_mate_escrow.json $(solana address -k target/deploy/sol_mate_escrow-keypair.json)"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.29.0",
    "@solana/spl-token": "^0.4.0",
    "@solana/web3.js": "^1.91.0"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "chai": "^4.3.4",
    "mocha": "^10.0.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
